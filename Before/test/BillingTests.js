const assert = require('chai').assert
const Vehicle=require ("../Vehicle.js")
const Rental=require ("../Rental.js")
const Customer=require ("../Customer.js")

/////////////////////////////////////////////////////////////
/////////// This is where your tests should go //////////////
/////////////////////////////////////////////////////////////
describe('The <<something>> should', () => {
  it('Match first expectation', () => {

    assert.equal(1,1);
  });

  it('Match second expectation', () => {

    assert.equal(2,2);
  });

});
